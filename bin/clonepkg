#!/usr/bin/env perl6
use v6;
use ArchLinux::AUR::Package::Clone;

sub MAIN(*@pkg --> Nil)
{
    clonepkg(@pkg);
}

sub USAGE(--> Nil)
{
    constant $HELP = q:to/EOF/.trim;
    Usage:
      clonepkg <pkg> <pkg>...
    EOF
    $HELP.say;
}

# vim: set filetype=perl6 foldmethod=marker foldlevel=0:
